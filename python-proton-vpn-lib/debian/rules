#!/usr/bin/make -f

export PYBUILD_NAME=python-proton-vpn-lib
#export PYBUILD_SYSTEM=pyproject
#export PYBUILD_SYSTEM=setuptools

%:
	mkdir -p proton/vpn/lib
	cp {{rust_triplet}}/release/libpython_proton_vpn_lib.so proton/vpn/lib/lib.abi3.so
	dh $@ --with python3 --buildsystem=pybuild
